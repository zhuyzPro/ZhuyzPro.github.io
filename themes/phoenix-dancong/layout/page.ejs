<article class="content-page">
  <header class="page-hero editorial-hero">
    <p class="eyebrow">凤凰单枞</p>
    <h1><%= page.title %></h1>
    <% if (page.description) { %><p><%= page.description %></p><% } %>
  </header>
  <div class="prose">
    <%- page.content %>
  </div>
</article>
